export const environment = {
  production: true,
  // Overridden per deployment — see docs/deployment.md. This default assumes
  // the API is reverse-proxied at /api on the same origin as the site.
  apiBaseUrl: '/api/v1',
};
